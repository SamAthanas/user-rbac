# Home Assistant server hostname or IP address
HA_SERVER_HOST=192.168.1.100

# SSH username for Home Assistant server
HA_SERVER_USER=root

# SSH password for Home Assistant server
HA_SERVER_PASSWORD=your_ssh_password_here

# SSH port (default: 22)
HA_SERVER_PORT=22

# Path to Home Assistant custom_components directory on the server
HA_SERVER_PATH=/config/custom_components

# =============================================================================
# Development Configuration
# =============================================================================

# Node.js environment for frontend development
NODE_ENV=development

# Frontend development server port (if running locally)
FRONTEND_DEV_PORT=3000

# =============================================================================
# Optional: Home Assistant API Configuration
# =============================================================================

# Home Assistant base URL (for API calls during development)
# HA_BASE_URL=http://192.168.1.100:8123

# Home Assistant Long-Lived Access Token (for API testing)
# HA_ACCESS_TOKEN=your_long_lived_access_token_here

# =============================================================================
# Optional: Logging Configuration
# =============================================================================

# Log level for development (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=DEBUG

# Enable verbose logging for RBAC component
# RBAC_DEBUG=true

# =============================================================================
# Security Notes
# =============================================================================
# 
# 1. NEVER commit the actual .env file to version control
# 2. Use strong passwords and consider SSH key authentication instead of passwords
# 3. Restrict SSH access to trusted networks only
# 4. Consider using Home Assistant Add-ons like "SSH & Web Terminal" for secure access
# 5. Long-lived access tokens should be created in Home Assistant UI under Profile
#
# =============================================================================
# Usage Instructions
# =============================================================================
#
# 1. Copy this file: cp .env.example .env
# 2. Update the values above with your actual Home Assistant server details
# 3. Run deployment scripts:
#    - ./scripts/deploy.sh (full deployment)
#    - ./scripts/deploy-frontend.sh (frontend only)
# 4. For development, you can also use these variables in your IDE or scripts
#
# =============================================================================
